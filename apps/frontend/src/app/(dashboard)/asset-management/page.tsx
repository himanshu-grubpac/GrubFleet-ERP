import { ModuleShell } from '@/components/modules/module-shell';

export default function AssetManagementPage() {
  return (
    <ModuleShell title="Asset management" description="Asset registry and lifecycle (planned)." />
  );
}
