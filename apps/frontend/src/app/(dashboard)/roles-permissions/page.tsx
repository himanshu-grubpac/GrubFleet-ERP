import { ModuleShell } from '@/components/modules/module-shell';

export default function RolesPermissionsPage() {
  return (
    <ModuleShell
      title="Roles & permissions"
      description="RBAC catalog, role assignments, and permission-aware UI (planned)."
    />
  );
}
